# 🗺️ ДОРОЖНАЯ КАРТА РЕФАКТОРИНГА

**Дата:** 2025-11-13
**Проект:** MPV2 Test System (MODX REVO)
**Цель:** Улучшение архитектуры, безопасности и поддерживаемости

---

## 🎯 СТРАТЕГИЧЕСКИЕ ЦЕЛИ

### Краткосрочные (1-2 недели):
1. ✅ Устранить критические уязвимости безопасности
2. ✅ Создать базовую инфраструктуру для рефакторинга
3. ✅ Уменьшить дублирование кода на 50%

### Среднесрочные (1-2 месяца):
1. ✅ Внедрить слой сервисов (Service Layer)
2. ✅ Создать репозитории (Repository Pattern)
3. ✅ Рефакторинг монолитных файлов

### Долгосрочные (3-6 месяцев):
1. ✅ Полная миграция на новую архитектуру
2. ✅ 80%+ покрытие тестами
3. ✅ API документация

---

## 📅 ДЕТАЛЬНЫЙ ПЛАН

---

## 🚀 ФАЗА 1: КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ (1-3 дня)

### Цель: Закрыть критические уязвимости безопасности

### Задача 1.1: CSRF Protection ⏱️ 4-6 часов

**Приоритет:** 🔴 КРИТИЧНО

**Файлы для изменения:**
- `core/components/testsystem/security/CsrfProtection.php` (создать)
- `assets/components/testsystem/ajax/testsystem.php`
- `core/elements/snippets/addTestForm.php`
- `core/elements/snippets/csvImportForm.php`
- `core/elements/snippets/manageCategories.php`
- `core/elements/snippets/authHandler.php`
- `assets/components/testsystem/js/tsrunner.js`

**Шаги:**
1. Создать класс `CsrfProtection`
2. Добавить генерацию токенов во все формы
3. Добавить валидацию в AJAX API
4. Обновить JavaScript для передачи токенов
5. Тестирование

**Критерий успеха:**
✅ Все POST запросы защищены CSRF токенами
✅ Атаки CSRF невозможны

---

### Задача 1.2: Улучшение валидации загрузки файлов ⏱️ 2-3 часа

**Приоритет:** 🔴 КРИТИЧНО

**Файлы:**
- `assets/components/testsystem/ajax/upload-image.php`
- `core/elements/snippets/csvImportForm.php`
- `core/elements/snippets/addTestForm.php`

**Шаги:**
1. Добавить проверку реального MIME типа через `getimagesize()`
2. Добавить `.htaccess` в upload директории
3. Валидация содержимого CSV/Excel файлов
4. Ограничение типов файлов на уровне сервера

**Критерий успеха:**
✅ Невозможно загрузить PHP файл как изображение
✅ Upload директории защищены от прямого доступа

---

### Задача 1.3: Закрытие IDOR уязвимостей ⏱️ 3-4 часа

**Приоритет:** 🔴 КРИТИЧНО

**Файлы:**
- `assets/components/testsystem/ajax/testsystem.php` (actions: getQuestion, deleteQuestion, updateQuestion)

**Шаги:**
1. Добавить проверку владельца при `getQuestion`
2. Добавить проверку прав при `deleteQuestion`
3. Добавить логирование подозрительных попыток доступа
4. Тестирование с разными ролями

**Критерий успеха:**
✅ Пользователь не может получить чужие вопросы
✅ Логируются попытки несанкционированного доступа

---

## 🛠️ ФАЗА 2: БАЗОВАЯ ИНФРАСТРУКТУРА (3-5 дней)

### Цель: Создать фундамент для рефакторинга

### Задача 2.1: Создание Service Layer ⏱️ 1-2 дня

**Приоритет:** 🟡 ВЫСОКИЙ

**Создать классы:**

```
core/components/testsystem/
├── bootstrap.php                  # Автозагрузчик
├── config/
│   └── config.php                # Конфигурация
├── services/
│   ├── AccessService.php         # Проверка прав
│   ├── AuthService.php           # Авторизация
│   ├── TestService.php           # Бизнес-логика тестов
│   ├── QuestionService.php       # Бизнес-логика вопросов
│   └── StatisticsService.php     # Статистика
├── repositories/
│   ├── BaseRepository.php        # Базовый репозиторий
│   ├── TestRepository.php
│   ├── QuestionRepository.php
│   └── UserRepository.php
├── helpers/
│   ├── HtmlHelper.php            # Экранирование
│   ├── UrlHelper.php             # Генерация URL
│   └── ValidationHelper.php      # Валидация
└── exceptions/
    └── TestSystemException.php   # Исключения
```

**Шаги:**
1. Создать структуру папок
2. Написать базовые классы
3. Создать PSR-4 автозагрузчик (composer)
4. Написать unit-тесты для сервисов

**Критерий успеха:**
✅ Все классы автоматически загружаются
✅ AccessService работает корректно
✅ BaseRepository выполняет запросы

---

### Задача 2.2: Рефакторинг проверки прав ⏱️ 1 день

**Приоритет:** 🟡 ВЫСОКИЙ

**Цель:** Заменить дублирующийся код на `AccessService`

**Файлы для изменения (17 файлов):**
- `testRunner.php` (6 мест)
- `addTestForm.php`
- `csvImportForm.php`
- `manageUsers.php` (4 места)
- `testsystem.php`
- `upload-image.php`
- `userMenu.php` (2 места)

**Было:**
```php
$roleStmt = $modx->prepare("SELECT mgn.`name` FROM ...");
$roleStmt->execute([':uid' => $userId]);
$roleNames = $roleStmt->fetchAll(PDO::FETCH_COLUMN);
$isAdmin = in_array('LMS Admins', $roleNames, true);
```

**Стало:**
```php
$accessService = new AccessService($modx);
$isAdmin = $accessService->isAdmin($userId);
$isExpert = $accessService->isExpert($userId);
$canEdit = $accessService->canEditTest($testId, $userId);
```

**Критерий успеха:**
✅ Удалено 500+ строк дублированного кода
✅ Проверка прав в одном месте
✅ Все функции работают как раньше

---

### Задача 2.3: Рефакторинг проверки авторизации ⏱️ 4 часа

**Приоритет:** 🟡 ВЫСОКИЙ

**Файлы (15 файлов):**
Все сниппеты с `hasSessionContext('web')`

**Было:**
```php
if (!$modx->user->hasSessionContext('web')) {
    $authUrl = $modx->makeUrl(...);
    return '<div class="alert">...</div>';
}
$userId = (int)$modx->user->get('id');
```

**Стало:**
```php
$authService = new AuthService($modx);
$guard = $authService->requireAuth();
if ($guard !== null) return $guard; // возвращает HTML ошибки

$userId = $authService->getUserId();
```

**Критерий успеха:**
✅ Удалено 400+ строк дублированного кода
✅ Единообразные сообщения об ошибках

---

## 🔧 ФАЗА 3: РЕФАКТОРИНГ МОНОЛИТОВ (5-7 дней)

### Цель: Разбить большие файлы на модули

### Задача 3.1: Рефакторинг testsystem.php ⏱️ 3 дня

**Приоритет:** 🔴 КРИТИЧНО

**Текущее состояние:**
- 3000+ строк
- 30+ actions в одном файле

**Целевое состояние:**

```
assets/components/testsystem/api/
├── index.php                      # Router
├── controllers/
│   ├── TestController.php         # Actions: getTestInfo, startTest, finishTest
│   ├── QuestionController.php     # Actions: saveQuestion, getQuestion, deleteQuestion
│   ├── SessionController.php      # Actions: getNextQuestion, submitAnswer
│   ├── FavoriteController.php     # Actions: toggleFavorite, getFavorites
│   └── PermissionController.php   # Actions: getPermissions, updatePermissions
└── middleware/
    ├── AuthMiddleware.php         # Проверка авторизации
    └── CsrfMiddleware.php         # Проверка CSRF
```

**Шаги:**
1. Создать ApiRouter для распределения actions
2. Создать контроллеры по доменам
3. Перенести логику из testsystem.php в контроллеры
4. Использовать сервисы вместо прямых SQL
5. Добавить middleware

**Критерий успеха:**
✅ testsystem.php < 200 строк (только router)
✅ Каждый контроллер < 300 строк
✅ Все API endpoints работают

---

### Задача 3.2: Рефакторинг testRunner.php ⏱️ 2 дня

**Приоритет:** 🟡 ВЫСОКИЙ

**Текущее состояние:**
- 733 строки
- Смешаны: бизнес-логика, SQL, генерация HTML

**Целевое состояние:**

```php
// testRunner.php (100-150 строк)
$testService = new TestService($modx);
$accessService = new AccessService($modx);
$authService = new AuthService($modx);

// Проверка авторизации
if ($guard = $authService->requireAuth()) {
    return $guard;
}

$userId = $authService->getUserId();
$resourceId = (int)$modx->resource->get('id');

// Режим: Knowledge Area
if ($knowledgeAreaId = $request->get('knowledge_area')) {
    return $testService->renderKnowledgeArea($knowledgeAreaId, $userId);
}

// Режим: Обычный тест
$test = $testService->getTestByResource($resourceId);

if (!$test) {
    return $modx->getChunk('testNotFound');
}

// Проверка доступа
if (!$accessService->canAccessTest($test['id'], $userId)) {
    return $modx->getChunk('accessDenied');
}

// Генерация вывода
return $modx->getChunk('testRunner', [
    'test' => $test,
    'canEdit' => $accessService->canEditTest($test['id'], $userId),
    'totalQuestions' => $testService->getQuestionsCount($test['id'])
]);
```

**Критерий успеха:**
✅ testRunner.php < 200 строк
✅ Логика в сервисах, не в сниппете
✅ HTML в chunks, не в PHP

---

### Задача 3.3: Рефакторинг csvImportForm.php ⏱️ 1 день

**Приоритет:** 🟢 СРЕДНИЙ

**Текущее состояние:** 482 строки

**Целевое состояние:**

```php
// csvImportForm.php (100-150 строк)
$importService = new ImportService($modx);
$accessService = new AccessService($modx);

// ...проверки...

if ($_POST && isset($_POST['import'])) {
    $file = $_FILES['csv_file'];
    $testId = (int)$_POST['test_id'];

    try {
        $result = $importService->importFromFile($file, $testId, $userId);
        return $modx->getChunk('importSuccess', $result);
    } catch (ImportException $e) {
        return $modx->getChunk('importError', ['error' => $e->getMessage()]);
    }
}

return $modx->getChunk('importForm', ['test' => $test]);
```

**Критерий успеха:**
✅ csvImportForm.php < 200 строк
✅ Логика импорта в ImportService
✅ Улучшена обработка ошибок

---

## 📝 ФАЗА 4: ТЕСТИРОВАНИЕ (2-3 дня)

### Цель: Покрытие тестами критической логики

### Задача 4.1: Unit тесты для сервисов ⏱️ 1 день

**Приоритет:** 🟡 ВЫСОКИЙ

**Создать тесты:**

```
tests/
├── Unit/
│   ├── Services/
│   │   ├── AccessServiceTest.php
│   │   ├── TestServiceTest.php
│   │   └── QuestionServiceTest.php
│   └── Repositories/
│       ├── TestRepositoryTest.php
│       └── QuestionRepositoryTest.php
└── phpunit.xml
```

**Покрытие:**
- AccessService: проверка прав (100%)
- TestService: CRUD операции (80%+)
- Repositories: SQL запросы (80%+)

**Критерий успеха:**
✅ 80%+ покрытие сервисного слоя
✅ Все тесты проходят

---

### Задача 4.2: Integration тесты ⏱️ 1 день

**Приоритет:** 🟢 СРЕДНИЙ

**Покрытие:**
- Прохождение теста (от start до finish)
- Импорт CSV
- Создание теста

**Критерий успеха:**
✅ Основные пользовательские сценарии покрыты

---

## 📚 ФАЗА 5: ДОКУМЕНТАЦИЯ (1-2 дня)

### Задача 5.1: API документация ⏱️ 1 день

**Создать:**
- `docs/API.md` - описание всех AJAX endpoints
- PHPDoc комментарии во всех классах
- README.md с инструкциями по развертыванию

**Критерий успеха:**
✅ Все API endpoints задокументированы
✅ Примеры запросов/ответов

---

### Задача 5.2: Архитектурная документация ⏱️ 4 часа

**Создать:**
- `docs/ARCHITECTURE.md` - описание новой архитектуры
- Диаграммы компонентов
- Описание паттернов

---

## 🔄 ФАЗА 6: ОПТИМИЗАЦИЯ (3-5 дней)

### Задача 6.1: Оптимизация БД ⏱️ 2 дня

**Шаги:**
1. Анализ медленных запросов (EXPLAIN)
2. Добавление индексов
3. Оптимизация N+1 проблем
4. Добавление кэширования (Redis/Memcached опционально)

**Критерий успеха:**
✅ Все запросы < 100ms
✅ Уменьшено количество запросов на 30%

---

### Задача 6.2: Frontend оптимизация ⏱️ 1 день

**Шаги:**
1. Минификация JS/CSS
2. Ленивая загрузка изображений
3. Сжатие ответов (gzip)

---

## 📊 МЕТРИКИ УСПЕХА

### Безопасность:
- ✅ 0 критических уязвимостей
- ✅ CSRF защита: 100%
- ✅ Input validation: 100%

### Качество кода:
- ✅ Дублирование: < 10% (было 32%)
- ✅ Средний размер файла: < 200 строк (было 400+)
- ✅ Цикломатическая сложность: < 10

### Тестирование:
- ✅ Покрытие unit тестами: 80%+
- ✅ Покрытие integration тестами: 60%+

### Производительность:
- ✅ Время загрузки страницы: < 1s
- ✅ Время ответа API: < 200ms

---

## 💰 ОЦЕНКА ТРУДОЗАТРАТ

| Фаза | Задач | Дней | Часов |
|------|-------|------|-------|
| Фаза 1: Критические исправления | 3 | 1-3 | 8-24 |
| Фаза 2: Базовая инфраструктура | 3 | 3-5 | 24-40 |
| Фаза 3: Рефакторинг монолитов | 3 | 5-7 | 40-56 |
| Фаза 4: Тестирование | 2 | 2-3 | 16-24 |
| Фаза 5: Документация | 2 | 1-2 | 8-16 |
| Фаза 6: Оптимизация | 2 | 3-5 | 24-40 |
| **ИТОГО** | **15** | **15-25** | **120-200** |

**Общая оценка:** 3-5 недель работы одного разработчика

---

## 🎯 РЕКОМЕНДУЕМЫЙ ПОРЯДОК ВЫПОЛНЕНИЯ

### Неделя 1: БЕЗОПАСНОСТЬ
- Фаза 1 (полностью)
- Начало Фазы 2 (базовые классы)

### Неделя 2-3: ИНФРАСТРУКТУРА
- Фаза 2 (полностью)
- Начало Фазы 3 (testsystem.php)

### Неделя 3-4: РЕФАКТОРИНГ
- Фаза 3 (полностью)
- Фаза 4 (unit тесты)

### Неделя 4-5: ДОРАБОТКА
- Фаза 4 (integration тесты)
- Фаза 5 (документация)
- Фаза 6 (оптимизация)

---

## ⚠️ РИСКИ И МИТИГАЦИЯ

### Риск 1: Поломка существующего функционала
**Вероятность:** Средняя
**Воздействие:** Высокое

**Митигация:**
- ✅ Создать staging окружение
- ✅ Написать integration тесты ДО рефакторинга
- ✅ Постепенная миграция (feature toggle)

### Риск 2: Превышение сроков
**Вероятность:** Средняя
**Воздействие:** Среднее

**Митигация:**
- ✅ Фокус на критичных задачах (Фаза 1-2)
- ✅ Оптимизацию можно отложить

### Риск 3: Недостаточная экспертиза
**Вероятность:** Низкая
**Воздействие:** Высокое

**Митигация:**
- ✅ Code review на каждый этап
- ✅ Консультации с архитектором

---

## 🏁 КРИТЕРИИ ГОТОВНОСТИ

### Минимально допустимый результат (MVP):
- ✅ Фаза 1 выполнена (безопасность)
- ✅ AccessService внедрен
- ✅ testsystem.php разбит на контроллеры

### Желаемый результат:
- ✅ Фазы 1-4 выполнены
- ✅ Покрытие тестами 80%+
- ✅ Документация готова

### Идеальный результат:
- ✅ Все фазы выполнены
- ✅ Производительность улучшена на 30%
- ✅ Техдолг уменьшен на 70%

---

## 📞 СЛЕДУЮЩИЕ ШАГИ

1. **Получить одобрение плана** от заказчика/тимлида
2. **Создать резервную копию БД и кода**
3. **Настроить staging окружение**
4. **Начать с Фазы 1, Задача 1.1** (CSRF Protection)

---

**Дата создания:** 2025-11-13
**Дата следующего review:** После каждой фазы
**Ответственный:** [Указать имя]

**Файл создан автоматически в рамках Этапа 1 аудита кода.**
