# 📊 СТРУКТУРА БАЗЫ ДАННЫХ - АНАЛИЗ

**Дата:** 2025-11-13
**Проект:** MPV2 Test System (MODX REVO)
**Префикс таблиц:** `modx_`

---

## 🗄️ ТАБЛИЦЫ ТЕСТОВОЙ СИСТЕМЫ

### 1. **modx_test_tests** (Основная таблица тестов)
**Назначение:** Хранит метаданные тестов
**Поля (предположительно из кода):**
- `id` - PRIMARY KEY
- `title` - Название теста
- `description` - Описание
- `resource_id` - Связь с MODX-ресурсом (страницей)
- `category_id` - Связь с категорией
- `mode` - ENUM('training', 'exam')
- `time_limit` - Ограничение по времени (минуты)
- `pass_score` - Проходной балл (%)
- `questions_per_session` - Количество вопросов за попытку
- `created_by` - ID создателя
- `publication_status` - ENUM('public', 'unlisted', 'private', 'draft')
- `is_active` - Активен/неактивен

**Используется в:**
- `testRunner.php:242-258`
- `testsystem.php:88-96`
- `testsList.php:28`
- `addTestForm.php`

---

### 2. **modx_test_questions** (Вопросы)
**Назначение:** Хранит вопросы тестов
**Поля:**
- `id` - PRIMARY KEY
- `test_id` - FOREIGN KEY → modx_test_tests
- `question_text` - Текст вопроса
- `question_type` - Тип (single, multiple, text)
- `explanation` - Объяснение правильного ответа
- `published` - Опубликован/снят с публикации
- `is_learning` - Обучающий материал
- `sort_order` - Порядок сортировки

**Используется в:**
- `testRunner.php:86,367`
- `testsystem.php:146,199,205,450,458`
- `csvImportForm.php:360`

---

### 3. **modx_test_answers** (Варианты ответов)
**Назначение:** Варианты ответов на вопросы
**Поля:**
- `id` - PRIMARY KEY
- `question_id` - FOREIGN KEY → modx_test_questions
- `answer_text` - Текст ответа
- `is_correct` - Правильный/неправильный
- `sort_order` - Порядок сортировки

**Используется в:**
- `testsystem.php:251,468`

---

### 4. **modx_test_categories** (Категории тестов)
**Назначение:** Иерархия категорий для организации тестов
**Поля:**
- `id` - PRIMARY KEY
- `name` - Название категории
- `description` - Описание
- `sort_order` - Порядок сортировки

**Используется в:**
- `manageCategories.php:37,82,102,109,128-129`
- `testsList.php:11`

---

### 5. **modx_test_sessions** (Сессии прохождения)
**Назначение:** Логирование попыток прохождения тестов
**Поля:**
- `id` - PRIMARY KEY
- `user_id` - FOREIGN KEY → modx_users
- `test_id` - FOREIGN KEY → modx_test_tests
- `score` - Результат в процентах
- `started_at` - Время начала
- `finished_at` - Время завершения
- `mode` - Режим (training/exam)

**Используется в:**
- `testsystem.php:404-405,571`
- `getSystemStats.php:14`

---

### 6. **modx_test_user_answers** (Ответы пользователей)
**Назначение:** Хранит ответы пользователей на вопросы
**Поля:**
- `id` - PRIMARY KEY
- `session_id` - FOREIGN KEY → modx_test_sessions
- `question_id` - FOREIGN KEY → modx_test_questions
- `answer_id` - FOREIGN KEY → modx_test_answers (для choice)
- `answer_text` - Текстовый ответ (для text questions)
- `is_correct` - Правильность ответа

**Используется в:**
- `testsystem.php:419,584`

---

### 7. **modx_test_permissions** (Права доступа)
**Назначение:** Управление доступом к приватным тестам
**Поля:**
- `id` - PRIMARY KEY
- `test_id` - FOREIGN KEY → modx_test_tests
- `user_id` - FOREIGN KEY → modx_users
- `can_edit` - Может редактировать
- `granted_at` - Время предоставления доступа

**Используется в:**
- `testRunner.php:344,441`
- `testsystem.php:129`
- `csvImportForm.php:102`

---

### 8. **modx_test_favorites** (Избранное)
**Назначение:** Избранные тесты пользователей
**Поля:**
- `id` - PRIMARY KEY
- `user_id` - FOREIGN KEY → modx_users
- `test_id` - FOREIGN KEY → modx_test_tests
- `added_at` - Дата добавления

**Используется в:**
- `myFavorites.php:31`
- `testsystem.php:2968`

---

### 9. **modx_test_knowledge_areas** (Области знаний)
**Назначение:** Персональные подборки тестов пользователей
**Поля:**
- `id` - PRIMARY KEY
- `user_id` - FOREIGN KEY → modx_users (владелец)
- `name` - Название области
- `description` - Описание
- `test_ids` - JSON массив ID тестов
- `questions_per_session` - Вопросов за попытку
- `is_active` - Активна/неактивна

**Используется в:**
- `testRunner.php:49`
- `testsystem.php:1649`

---

### 10. **modx_test_user_stats** (Статистика пользователей)
**Назначение:** Агрегированная статистика для лидерборда
**Поля:**
- `user_id` - PRIMARY KEY → modx_users
- `total_score` - Общий балл
- `tests_completed` - Завершено тестов
- `avg_score` - Средний балл
- `last_activity_date` - Последняя активность
- `current_streak` - Текущая серия

**Используется в:**
- `leaderboard.php:18,65,81`
- `leaderboardCompact.php:21`

---

### 11. **modx_test_category_stats** (Статистика по категориям)
**Назначение:** Лидерборд по категориям
**Поля:**
- `id` - PRIMARY KEY
- `user_id` - FOREIGN KEY → modx_users
- `category_id` - FOREIGN KEY → modx_test_categories
- `total_score` - Балл в категории
- `tests_completed` - Тестов пройдено

**Используется в:**
- `leaderboardCategories.php:13,56`

---

### 12. **modx_test_notifications** (Уведомления)
**Назначение:** Системные уведомления для пользователей
**Поля (предположительно):**
- `id` - PRIMARY KEY
- `user_id` - FOREIGN KEY → modx_users
- `message` - Текст уведомления
- `is_read` - Прочитано/нет
- `created_at` - Дата создания

**Статус:** Упоминается в коде, детали неизвестны

---

## 🔗 СВЯЗИ С ТАБЛИЦАМИ MODX

### **modx_users** (Стандартная таблица MODX)
Связана с:
- modx_test_sessions
- modx_test_permissions
- modx_test_favorites
- modx_test_knowledge_areas
- modx_test_user_stats
- modx_test_category_stats

### **modx_user_attributes** (Профили пользователей)
Используется для:
- Получение имён пользователей в лидербордах

### **modx_member_groups** (Группы пользователей)
Используется для:
- Проверка ролей (LMS Admins, LMS Experts)

### **modx_membergroup_names** (Названия групп)
Используется для:
- Проверка прав доступа

### **modx_site_content** (Ресурсы MODX)
Связь:
- `modx_test_tests.resource_id` → `modx_site_content.id`
- Каждый тест привязан к странице

---

## 📈 ДИАГРАММА СВЯЗЕЙ (ERD)

```
modx_test_tests (1) ──< (∞) modx_test_questions (1) ──< (∞) modx_test_answers
       │                           │
       │                           │
       │ (1)                       │ (1)
       │                           │
       ├──< (∞) modx_test_sessions ──> modx_test_user_answers (∞) >──┘
       │            │
       │            │ (∞)
       │            │
       │            └──> (1) modx_users <──┐
       │                      │             │
       │ (∞)                  │ (1)         │
       │                      │             │
       ├──< modx_test_permissions           │
       │                      │             │
       │ (∞)                  │             │
       │                      │             │
       └──< modx_test_favorites             │
                              │             │
modx_test_categories (1) ──< │ (∞)         │
       │                      │             │
       │ (1)                  │ (1)         │
       │                      │             │
       └──< modx_test_category_stats ──────┘
                              │
                              │ (1)
                              │
                    modx_test_user_stats
```

---

## ⚠️ ПРОБЛЕМЫ И РИСКИ

### 🔴 Критические:
1. **Отсутствие внешних ключей (FK)** - связи только логические, нет каскадного удаления
2. **Хардкоженные имена таблиц** - в коде встречается `modx_` вместо `$prefix`
3. **Нет транзакций** - при удалении теста могут остаться orphan записи
4. **Дублирование логики проверки прав** - код повторяется в 10+ местах

### 🟡 Средние:
1. **Отсутствие индексов** (требуется анализ EXPLAIN)
2. **JSON поля** (`test_knowledge_areas.test_ids`) - сложность запросов
3. **Денормализация** (`test_user_stats`) - требует синхронизации
4. **Нет миграций** - изменения структуры БД не версионируются

### 🟢 Низкие:
1. Отсутствие `created_at`/`updated_at` в некоторых таблицах
2. Нет soft delete (флаг `deleted_at`)
3. Нет аудита изменений

---

## 📋 РЕКОМЕНДАЦИИ

### Немедленно:
1. ✅ **Создать резервную копию БД**
2. ✅ **Добавить внешние ключи с ON DELETE CASCADE**
3. ✅ **Унифицировать использование префикса таблиц**

### В ближайшее время:
1. Создать миграции для всех таблиц
2. Добавить недостающие индексы
3. Добавить поля `created_at`, `updated_at`, `deleted_at`
4. Обернуть критические операции в транзакции

### Долгосрочно:
1. Рефакторинг репозиториев (единая точка доступа к БД)
2. Внедрить ORM (или Query Builder)
3. Версионирование схемы БД

---

**Файл создан автоматически в рамках Этапа 1 аудита кода.**
