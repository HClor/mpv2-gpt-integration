# Аудит соответствия текущего состояния — 2026-03-13

> **База сравнения:** `AUDIT_2026-02-22_testsystem.md` и `AUDIT_2026-02-24_core_elements.md`.
> 
> **Цель:** зафиксировать, что из ранее заявленного выполнено/актуально на текущий момент, и что остаётся в работе.

## Область проверки

- `assets/components/testsystem/`
- `core/components/testsystem/`
- `core/elements/chunks/`
- `core/elements/templates/`
- `core/elements/plugins/`

---

## 1. СООТВЕТСТВИЕ AUDIT_2026-02-22_testsystem.md

### 1.1 Критические проблемы (раздел 1) — СООТВЕТСТВУЕТ

| Пункт из аудита 2026-02-22 | Статус на 2026-03-13 | Комментарий |
|---|---|---|
| Удаление мёртвых inline-case из `testsystem.php` | ✅ Подтверждено | В `switch` осталось 6 кейсов (утилитарные/материалы), основная маршрутизация через `ControllerFactory`. |
| Дубликат `getTestPermissions` | ✅ Подтверждено | В `testsystem.php` дубликатов `case 'getTestPermissions'` нет, действие роутится через фабрику контроллеров. |
| `.htaccess` в `ajax/` | ✅ Подтверждено | Файл присутствует, листинг отключён, доступ к служебным расширениям ограничен. |

### 1.2 Миграция inline-case в контроллеры (раздел 2) — СООТВЕТСТВУЕТ

Статус миграций из разделов 2.1–2.5, 2.7, 2.8 подтверждён: соответствующие actions присутствуют в `ControllerFactory` и обрабатываются контроллерами.

**Актуальный остаток inline-case в `testsystem.php`:**
- `getApiVersion`
- `getCsrfToken`
- `getParentUri`
- `getMaterialsList`
- `getMaterial`
- `saveMaterial`

Это соответствует ранее зафиксированному решению оставить material-read/write логику вне старой таблицы и обрабатывать отдельно.

### 1.3 Дублирование upload-логики (раздел 3) — БЕЗ ИЗМЕНЕНИЙ

| Пункт из аудита 2026-02-22 | Статус на 2026-03-13 | Комментарий |
|---|---|---|
| Выделить общий `UploadService` | ❌ Не выполнено | Отдельного `UploadService.php` не обнаружено; `upload-image.php` и `upload-document.php` остаются раздельными и крупными. |

### 1.4 CSS/JS/безопасность/фазы (разделы 4–8) — ЧАСТИЧНО

- **Фаза 7 (CSS, низкий приоритет)** — остаётся невыполненной в исходной формулировке аудита.
- Общая архитектурная линия (контроллеры в `assets`, гибридная обработка материалов, без DB-триггеров) сохраняется.

### 1.5 Фактические метрики (по состоянию на 2026-03-13)

- `assets/components/testsystem/ajax/testsystem.php`: **549 строк**, **6 case-блоков**.
- Файлов в `assets/components/testsystem/`: **44**.
- Файлов в `core/components/testsystem/`: **56**.

---

## 2. СООТВЕТСТВИЕ AUDIT_2026-02-24_core_elements.md

### 2.1 Исправленные проблемы (раздел 1) — В ОСНОВНОМ СООТВЕТСТВУЕТ

| Пункт из аудита 2026-02-24 | Статус на 2026-03-13 | Комментарий |
|---|---|---|
| XSS-правки в чанках/шаблонах | ✅ Подтверждено выборочно | Ключевые места с `| escape` и безопасной вставкой сохранены. |
| DOM XSS (`scripts.tpl`, `tsScripts.tpl`) | ✅ Подтверждено | Сохранены безопасные паттерны (`text()`, `textContent`). |
| CSS comment bug в `tsHead.tpl` | ✅ Подтверждено | Некорректный комментарий не обнаружен. |
| Switch fallthrough в `content.tpl` | ✅ Подтверждено | `break` присутствуют во всех ветках `switch`. |
| CSRF в polling уведомлений | ✅ Подтверждено | Заголовок `X-CSRF-Token` передаётся. |

### 2.2 Нерешённые проблемы (раздел 2) — ПО-ПРЕЖНЕМУ АКТУАЛЬНЫ

| Нерешённый пункт из 2026-02-24 | Статус на 2026-03-13 | Комментарий |
|---|---|---|
| Смешение Fenom и MODX-тегов | ⚠️ Актуально | В `tsHead.tpl`, `tsHeader.tpl`, `tsFooter.tpl`, `LMS_Bootstrap_5.tpl`, `TestSystem.tpl` смешанный синтаксис остаётся. |
| Bootstrap 3/5 конфликт | ⚠️ Актуально | В siteExtra-чанках присутствуют BS3-классы при подключении BS5 в `head.tpl`. |
| Хардкод ID ресурсов | ⚠️ Актуально | Встречается `makeUrl(<id>)` и `[[~id]]` в `tsHeader.tpl`, `tsFooter.tpl`, `learning-materials-template-v2.html` и др. |
| PHP внутри шаблонов | ⚠️ Актуально | `learning-materials-template.html` и `learning-materials-template-v2.html` по-прежнему содержат `<?php ... ?>`. |
| Устаревшие шаблоны | ⚠️ Актуально | v1/v2 остаются в репозитории; `base.tpl` также присутствует. |
| Сырой вывод `jsTV/cssTV` | ⚠️ Актуально | В `tsHead.tpl` и `tsScripts.tpl` вывод TV-полей остаётся без дополнительной фильтрации. |
| Дублирование шаблонов | ⚠️ Актуально | Пары дублирующих файлов из аудита не рефакторены. |
| `<script>` в `<head>` | ⚠️ Актуально | Bootstrap JS всё ещё подключён внутри `head.tpl`. |

---

## 3. СВОДКА ПО ТОЧКЕ РАЗВИТИЯ

### Что подтверждённо стабилизировано

- Ключевая декомпозиция API на контроллеры выполнена и держится.
- Критические баги из аудита TestSystem (мёртвый код, дубликат кейса, hardening `ajax/`) закрыты и не регресснули.
- Ключевые security-правки core/elements (DOM XSS, CSRF polling, switch fallthrough) сохранены.

### Что остаётся «долгом архитектуры/поддержки»

1. **Template-layer tech debt:** смешение Fenom/MODX + legacy шаблоны с PHP.
2. **UI consistency debt:** конфликт Bootstrap 3/5.
3. **Config debt:** хардкод resource ID вместо настроек.
4. **Upload debt:** отсутствие единого `UploadService` и дублирование логики.
5. **Security-hardening debt:** сырой вывод TV (нужны ограничения ролей + CSP/политики).

---

## 4. РЕКОМЕНДУЕМЫЕ СЛЕДУЮЩИЕ ШАГИ (приоритет)

### P1 — Безопасность и поддерживаемость

1. Вынести `cssTV/jsTV` в безопасный pipeline (роль admin-only + whitelist + CSP).
2. Завершить унификацию шаблонов (убрать PHP из v1/v2, закрепить v3 как единственный путь).

### P2 — Архитектура шаблонов

3. Убрать смешение Fenom/MODX в целевых 5 файлах.
4. Вынести resource ID в системные настройки/конфиг.

### P3 — Технический долг

5. Сделать общий `UploadService` и сократить дубли в `upload-image.php`/`upload-document.php`.
6. Привести siteExtra к единой версии Bootstrap (предпочтительно BS5).

---

## 5. ИТОГ

Текущее состояние **высоко соответствует** зафиксированным результатам аудитов от 2026-02-22 и 2026-02-24: критические исправления удержаны, архитектурный вектор сохранён.

Основной незакрытый объём — это **не аварийные баги**, а **плановый техдолг слоя шаблонов/фронта и конфигурации**.
